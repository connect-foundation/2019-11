import React from 'react'

import Carousel from '../components/Molecules/Carousel'

export default {
    title: 'Carousel',
};

export const carousel = () => {
    return (
        <Carousel/>
    )
}