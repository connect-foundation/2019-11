import React, { useState } from 'react'

import InputBox from '../../components/Atoms/InputWithLimit'

export default {
    title: 'Atoms|inputWithLimits',
};

// export const inputBox = () => {

//     const [value, setValue] = useState('값')

//     return (
//         <InputBox size={2.3} value={value} onChange={setValue} hint={'글자 제한이 있는 박스'} limit={50} />
//     )
// }