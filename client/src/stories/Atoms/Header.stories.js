import React from 'react'

import Header from '../../components/Atoms/Header'

export default {
    title: 'Atoms|Header',
};

export const header = () => {
    return (
        <Header text={'헤 더'} />
    )
}